//
// Created by sfulham on 4/10/20.
//

#include "Buffer.h"

#include "Internal/Vulkan/VulkanBuffer.h"

namespace Internal
{
    /*SharedPointer<VertexBuffer> VertexBuffer::Create(float *vertices, uint32_t size)
    {
        return createSharedPointer<VulkanVertexBuffer>(vertices, size);
    }

    SharedPointer<VertexBuffer> VertexBuffer::Create(uint32_t size)
    {
        return createSharedPointer<VulkanVertexBuffer>(nullptr, size);
    }*/
}